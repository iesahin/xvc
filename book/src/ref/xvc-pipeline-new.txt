xvc-pipeline-new 
Add a new pipeline

USAGE:
    xvc pipeline new [OPTIONS] --name <NAME>

OPTIONS:
    -h, --help                 Print help information
    -n, --name <NAME>          Name of the pipeline this command applies to
        --set-default          set this to default
    -w, --workdir <WORKDIR>    default working directory
